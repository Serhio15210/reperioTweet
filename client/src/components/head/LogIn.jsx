import React from 'react';
import {Button} from "react-bootstrap";

const LogIn = () => {
    return (
            <Button variant="outline-light" id="right-panel-link">Log In</Button>
    );
};

export default LogIn;